# Luma Todo · 第一版产品说明

## 产品定位

面向 Windows 的轻量桌面待办，主要服务工作任务，兼顾生活事项。它强调“随手记、一直看得见、需要时才展开”，而不是做成重型项目管理工具。

## 核心界面

默认显示半透明桌面小组件，包含快速输入、今日待办、项目进度与未来重要节点。点击日历按钮后，同一窗口向左展开月历；再次点击或按 Esc 收起。

## 任务规则

- 没有日期的内容进入今日待办。
- 只有日期的任务进入未来待办，到期当天自动进入今日待办。
- 包含具体时间的任务同步为 Google Calendar 日程，创建后选择提醒时间。
- 普通任务同步到 Google Tasks。
- 未完成任务次日继续留在今日待办，并显示顺延天数。
- 完成表示打横线并折叠；删除才从两端移除。

## 项目规则

- 项目只有名称与颜色。
- 新任务默认进入未分类，可拖拽到项目。
- 有时间的任务自动排序，无时间任务允许手动排列。
- 本地项目对应 Google Tasks 的任务列表；颜色不上传。

## 同步规则

- 只由本地 Todo 创建的 Calendar 日程参与双向同步，避免导入整个个人日历。
- Google Tasks 中新建的任务会同步回本地。
- Google Calendar 负责已同步定时任务的提醒，本地不重复通知。
- 离线冲突默认采用最后修改优先，并保留本地备份用于恢复。

## 隐私与数据

任务首先保存在本机。应用每日创建一次自动备份，并允许手动导出 JSON。AI 日期识别和 Google 同步均应在设置中明确展示授权状态。
